resources:
- name: github-repo
  type: git
  source:
    uri: https://github.com/raashidjunaid1/try-concourse.git
    branch: master

jobs:
- name: test
  plan:
  - get: github-repo
    trigger: true
  - task: a
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: node
          tag: alpine
        
      inputs:
      - name: github-repo

      run:
        path: sh
        args:
        - -exec
        - |
            cd github-repo/app
            npm install
            npm test

- name: build
  plan:
  - get: github-repo
    passed: [test]
    trigger: true
  - task: a
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: node
          tag: alpine
    
      inputs:
      - name: github-repo

      outputs:
      - name: public

      run:
        path: sh
        args:
        - -exec
        - |
            cd github-repo/app
            npm install
            npm run build
            cp public public 
